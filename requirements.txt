# Test/import-time dependencies for CI (.github/workflows/python-app.yml).
# Each service under */app/ has its own requirements.txt for deployment;
# this file only needs enough to import and unit-test them.
fastapi==0.115.6
pydantic==2.10.3
httpx==0.28.1
# Parses action.yml in tests/test_marketplace_action.py. Absent, that module
# skipped in CI and 29 guards on the Marketplace action never ran.
PyYAML==6.0.3
playwright==1.48.0
axe-playwright-python==0.1.8
stripe==15.4.0
google-cloud-firestore==2.28.1
x402[evm,extensions,svm]==2.22.0
# The svm extra imports solana.rpc.api, which solana 0.40 removed; pin the
# last line that still has it.
solana==0.36.12
# Coinbase CDP facilitator auth: per-request JWTs for api.cdp.coinbase.com
# (mainnet x402 settlement + the x402 Bazaar listing).
cdp-sdk==1.48.1
